---
description: Show RAG database statistics — events, embeddings, chunks, projects, tools, languages
allowed-tools: mcp__plugin_claude-rag_rag__stats
---

# RAG Statistics

Use `mcp__plugin_claude-rag_rag__stats` to fetch and display the complete RAG database dashboard.
